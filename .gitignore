# SwiftPM build output
.build/

# Packaged app + DMG (regenerated by scripts/make_app.sh and scripts/make_dmg.sh)
dist/

# Vendored whisper.cpp source is a shallow clone pinned in scripts/bundle_whisper.sh
# (re-cloned on demand); only the built static server in vendor/whisper-bin/ is tracked.
vendor/whisper.cpp/

# macOS
.DS_Store
.claude/worktrees/
graphify-out/
